# Animated Drawings

## 📖 คำอธิบายโปรเจค
โปรเจคนี้ใช้ Animated Drawings จาก Meta AI (Facebook Research) สำหรับการแปลงภาพวาดหรือภาพการ์ตูนให้เคลื่อนไหวได้ สามารถสร้างแอนิเมชั่นจากภาพวาดแบบ 2D และส่งออกเป็นวิดีโอหรือ GIF ได้

## ✨ ฟีเจอร์หลัก
- ✅ แปลงภาพวาดให้เคลื่อนไหวได้แบบอัตโนมัติ
- ✅ รองรับการแสดงแอนิเมชั่นแบบ Interactive
- ✅ ส่งออกเป็นไฟล์ MP4 หรือ GIF
- ✅ ใช้ AI ในการตรวจจับโครงสร้างของตัวละคร
- ✅ รองรับท่าทางและการเคลื่อนไหวที่หลากหลาย

## 🛠️ เทคโนโลยีที่ใช้
- **Animated Drawings** - Library จาก Meta AI/Facebook Research
- **Python 3.8.13** - เวอร์ชันที่รองรับ
- **PyTorch** - สำหรับโมเดล AI
- **OpenGL** - สำหรับ Rendering

## 📦 การติดตั้ง

### วิธีที่ 1: ติดตั้งผ่าน Conda (แนะนำ)

```bash
# สร้าง Environment ใหม่
conda create --name animated_drawings python=3.8.13
conda activate animated_drawings

# Clone Repository
git clone https://github.com/facebookresearch/AnimatedDrawings.git
cd AnimatedDrawings

# ติดตั้ง Package
pip install -e .
```

### วิธีที่ 2: ติดตั้งผ่าน pip

```bash
pip install animated-drawings
```

## 🚀 วิธีการใช้งาน

### 1. แสดงแอนิเมชั่นแบบ Interactive

```python
from animated_drawings import render
render.start('./examples/config/mvc/interactive_window_example.yaml')
```

หน้าต่างแอนิเมชั่นจะเปิดขึ้น และสามารถควบคุมการเคลื่อนไหวได้แบบเรียลไทม์

### 2. ส่งออกเป็นไฟล์ MP4

```python
from animated_drawings import render
render.start('./examples/config/mvc/export_mp4_example.yaml')
```

ไฟล์วิดีโอจะถูกสร้างและบันทึกลงในโฟลเดอร์ที่กำหนด

### 3. ส่งออกเป็นไฟล์ GIF

```python
from animated_drawings import render
render.start('./examples/config/mvc/export_gif_example.yaml')
```

ไฟล์ GIF จะถูกสร้างพร้อมใช้งาน

## 📁 โครงสร้างไฟล์ Config

ไฟล์ YAML config ประกอบด้วย:
- **character**: กำหนดภาพวาดและโครงสร้างตัวละคร
- **motion**: กำหนดท่าทางและการเคลื่อนไหว
- **output**: กำหนดรูปแบบการส่งออก (interactive, mp4, gif)

### ตัวอย่าง Config File

```yaml
scene:
  ADD_FLOOR: true
  CAMERA_POS: [0, 1.0, 3.5]
  CAMERA_FWD: [0, -0.3, -1]

controller:
  MODE: 'video_render'  # หรือ 'interactive'

view:
  OUTPUT_VIDEO_PATH: './output/animation.mp4'
  OUTPUT_VIDEO_FPS: 30
```

## 🎨 การสร้างแอนิเมชั่นจากภาพวาดของคุณเอง

1. **เตรียมภาพวาด**: ภาพควรมีพื้นหลังสีขาว ตัวละครชัดเจน
2. **สร้าง Config File**: กำหนด path ของภาพและพารามิเตอร์
3. **ระบุจุดสำคัญ**: (Optional) กำหนดตำแหน่งข้อต่อของตัวละคร
4. **รัน Render**: เรียกใช้ `render.start()` พร้อม config file

## 💡 ตัวอย่างการใช้งาน

### แอนิเมชั่นเดิน (Walking)
```python
from animated_drawings import render
render.start('./examples/config/mvc/walking_animation.yaml')
```

### แอนิเมชั่นเต้น (Dancing)
```python
from animated_drawings import render
render.start('./examples/config/mvc/dancing_animation.yaml')
```

### แอนิเมชั่นกระโดด (Jumping)
```python
from animated_drawings import render
render.start('./examples/config/mvc/jumping_animation.yaml')
```

## ⚙️ ปัญหาที่พบบ่อยและวิธีแก้

### ปัญหา: Import Error
```bash
# แก้ไขโดยติดตั้ง dependencies เพิ่มเติม
pip install numpy opencv-python Pillow scipy
```

### ปัญหา: OpenGL Error
```bash
# ติดตั้ง PyOpenGL
pip install PyOpenGL PyOpenGL_accelerate
```

### ปัญหา: CUDA Error (ถ้าใช้ GPU)
```bash
# ติดตั้ง PyTorch ที่รองรับ CUDA
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
```

## 📝 หมายเหตุ

- ต้องใช้ Python 3.8.13 เท่านั้น (เวอร์ชันอื่นอาจมีปัญหา)
- ภาพวาดควรมีพื้นหลังที่ชัดเจนเพื่อผลลัพธ์ที่ดี
- การ Render วิดีโออาจใช้เวลานานขึ้นอยู่กับความยาวและความละเอียด
- รองรับรูปแบบภาพ: PNG, JPG, JPEG

## 🔗 ทรัพยากรเพิ่มเติม

- [GitHub Repository](https://github.com/facebookresearch/AnimatedDrawings)
- [Paper](https://arxiv.org/abs/2303.12741)
- [Demo Website](https://sketch.metademolab.com/)
- [Documentation](https://github.com/facebookresearch/AnimatedDrawings/blob/main/docs/README.md)

## 📊 ข้อกำหนดของระบบ

**ขั้นต่ำ:**
- Python 3.8.13
- RAM 4GB
- Disk Space 2GB

**แนะนำ:**
- Python 3.8.13
- RAM 8GB+
- GPU (สำหรับ Rendering ที่เร็วขึ้น)
- Disk Space 5GB+

## 🎯 การใช้งานขั้นสูง

### Custom Motion Sequences
สร้าง motion sequence ของคุณเองโดยแก้ไขไฟล์ BVH (BioVision Hierarchy)

### Batch Processing
ประมวลผลภาพวาดหลายภาพพร้อมกัน:
```python
import os
from animated_drawings import render

images = ['char1.png', 'char2.png', 'char3.png']
for img in images:
    config = f'./configs/{img.split(".")[0]}.yaml'
    render.start(config)
```

---

**Credit:** Animated Drawings by Meta AI Research / Facebook Research