# 622 - Generate Text with ChatGPT

![Python](https://img.shields.io/badge/Python-3.x-blue)
![OpenAI](https://img.shields.io/badge/OpenAI-GPT--4o--mini-green)
![API](https://img.shields.io/badge/API-Integration-orange)

สร้างข้อความอัตโนมัติด้วย ChatGPT API โดยใช้ GPT-4o-mini สำหรับการสนทนาและตอบคำถามภาษาไทย

## 📖 คำอธิบาย

โปรเจคนี้เป็นตัวอย่างการใช้งาน OpenAI ChatGPT API เพื่อสร้างข้อความและตอบคำถาม โดยใช้โมเดล **GPT-4o-mini** ซึ่งเป็นโมเดลที่มีประสิทธิภาพสูงและประหยัดต้นทุน รองรับภาษาไทยได้ดีเยี่ยม

## ✨ ฟีเจอร์

- ✅ เชื่อมต่อกับ OpenAI API (GPT-4o-mini)
- ✅ รองรับภาษาไทยเต็มรูปแบบ
- ✅ กำหนด System Prompt ได้
- ✅ รับคำตอบจากโมเดล AI
- ✅ ปรับแต่ง Parameters ได้ (temperature, n, stop)
- ✅ ใช้งานง่าย เหมาะสำหรับผู้เริ่มต้น

## 🔧 เทคโนโลยีที่ใช้

- **OpenAI API** - GPT-4o-mini Model
- **Python 3.x** - ภาษาโปรแกรม
- **openai Library** - Python SDK สำหรับ OpenAI

## 📦 การติดตั้ง

```bash
pip install openai
```

## 🔑 การตั้งค่า API Key

### วิธีที่ 1: ใส่ตรงในโค้ด (ไม่แนะนำสำหรับ Production)
```python
client = OpenAI(api_key="your-api-key-here")
```

### วิธีที่ 2: ใช้ Environment Variable (แนะนำ)
```bash
# Windows
set OPENAI_API_KEY=your-api-key-here

# macOS/Linux
export OPENAI_API_KEY=your-api-key-here
```

```python
client = OpenAI()  # จะอ่าน API key จาก environment
```

### วิธีที่ 3: ใช้ไฟล์ .env
```bash
pip install python-dotenv
```

สร้างไฟล์ `.env`:
```
OPENAI_API_KEY=your-api-key-here
```

ในโค้ด:
```python
from dotenv import load_dotenv
load_dotenv()
client = OpenAI()  # จะอ่านจาก .env
```

## 🚀 การใช้งาน

### การใช้งานพื้นฐาน

```python
from openai import OpenAI

client = OpenAI(api_key="your-api-key-here")

response = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=[
        {"role": "system", "content": "คุณคือนักปรัญญา"},
        {"role": "user", "content": "ไก่ย่างห้าดาวคืออะไร?"}
    ],
    n=1,
    stop=None
)

message = response.choices[0].message.content
print(message)
```

### ตัวอย่างผลลัพธ์

```
ไก่ย่างห้าดาวเป็นเมนูอาหารที่มีรสชาติอร่อย โดยทั่วไปหมายถึงไก่ย่างที่มีคุณภาพสูง 
ปรุงด้วยเครื่องเทศหลากหลายชนิด มีรสชาติเข้มข้น หอมกรุ่น และมีเนื้อสัมผัสที่นุ่มชุ่มฉ่ำ
```

## ⚙️ Parameters ที่สำคัญ

### 1. Model
```python
model="gpt-4o-mini"  # เร็ว ประหยัด เหมาะสำหรับงานทั่วไป
model="gpt-4o"       # แม่นยำสูงสุด แต่ราคาแพงกว่า
model="gpt-3.5-turbo" # เร็วที่สุด ราคาถูกที่สุด
```

### 2. Messages Structure
```python
messages=[
    {"role": "system", "content": "คุณคือ..."},    # บุคลิกของ AI
    {"role": "user", "content": "คำถามจากผู้ใช้"},  # คำถาม
    {"role": "assistant", "content": "คำตอบ"},    # ประวัติการตอบ (optional)
]
```

### 3. n (จำนวนคำตอบ)
```python
n=1  # รับคำตอบ 1 ข้อ (ค่าเริ่มต้น)
n=3  # รับคำตอบ 3 ข้อแตกต่างกัน
```

### 4. Temperature (ความสร้างสรรค์)
```python
temperature=0.0   # คำตอบแน่นอน เหมาะสำหรับข้อมูลจริง
temperature=0.7   # สมดุล (ค่าเริ่มต้น)
temperature=1.0   # สร้างสรรค์สูง เหมาะสำหรับงานเขียนเชิงสร้างสรรค์
```

### 5. Max Tokens
```python
max_tokens=100    # จำกัดความยาวคำตอบ
max_tokens=2000   # คำตอบยาว
```

## 📝 ตัวอย่างการใช้งาน

### 1. Chatbot ภาษาไทย
```python
response = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=[
        {"role": "system", "content": "คุณคือผู้ช่วยภาษาไทยที่เป็นมิตร"},
        {"role": "user", "content": "สวัสดีครับ วันนี้อากาศเป็นอย่างไร?"}
    ]
)
```

### 2. แปลภาษา
```python
response = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=[
        {"role": "system", "content": "คุณคือนักแปลภาษา แปลจากอังกฤษเป็นไทย"},
        {"role": "user", "content": "Hello, how are you today?"}
    ]
)
```

### 3. สรุปข้อความ
```python
response = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=[
        {"role": "system", "content": "สรุปข้อความให้กระชับ"},
        {"role": "user", "content": "ข้อความยาวๆ ที่ต้องการสรุป..."}
    ]
)
```

### 4. เขียนโค้ด
```python
response = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=[
        {"role": "system", "content": "คุณคือโปรแกรมเมอร์ Python"},
        {"role": "user", "content": "เขียนฟังก์ชันคำนวณเลขฟีโบนัชชี"}
    ]
)
```

## 💰 ราคา (GPT-4o-mini)

- **Input**: $0.150 / 1M tokens
- **Output**: $0.600 / 1M tokens

**ตัวอย่าง**: การสนทนา 100 ครั้ง (50 words ต่อครั้ง) ≈ $0.05-0.10

## 🎯 Use Cases

1. **Chatbot** - ระบบตอบคำถามอัตโนมัติ
2. **แปลภาษา** - แปลเอกสารภาษาต่างๆ
3. **สรุปข้อความ** - สรุปบทความหรือเอกสาร
4. **เขียนเนื้อหา** - สร้าง content marketing
5. **ตอบอีเมล** - ร่างอีเมลอัตโนมัติ
6. **ช่วยเขียนโค้ด** - แนะนำและแก้ไขโค้ด

## ⚠️ ข้อควรระวัง

1. **API Key Security**
   - ❌ ห้ามเผยแพร่ API Key บน GitHub
   - ✅ ใช้ Environment Variable หรือ .env
   - ✅ เพิ่ม `.env` เข้า `.gitignore`

2. **Rate Limits**
   - GPT-4o-mini: 500 requests/minute
   - ควรมี error handling สำหรับ rate limit

3. **Token Limits**
   - GPT-4o-mini: 128,000 tokens context window
   - 1 token ≈ 0.75 คำภาษาอังกฤษ
   - 1 token ≈ 0.5 คำภาษาไทย

4. **ค่าใช้จ่าย**
   - ตรวจสอบการใช้งานที่ [OpenAI Dashboard](https://platform.openai.com/usage)
   - ตั้ง Usage Limits เพื่อควบคุมค่าใช้จ่าย

## 🐛 การแก้ปัญหา

### Error: Incorrect API key provided
```python
# ตรวจสอบ API Key ว่าถูกต้อง
# ดูได้ที่: https://platform.openai.com/api-keys
```

### Error: Rate limit exceeded
```python
import time
try:
    response = client.chat.completions.create(...)
except Exception as e:
    time.sleep(60)  # รอ 1 นาที
    response = client.chat.completions.create(...)
```

### Error: Maximum context length exceeded
```python
# ลดความยาว messages หรือใช้ max_tokens
max_tokens=500  # จำกัดความยาวคำตอบ
```

## 📚 เอกสารเพิ่มเติม

- [OpenAI API Documentation](https://platform.openai.com/docs)
- [GPT-4o-mini Guide](https://platform.openai.com/docs/models/gpt-4o-mini)
- [OpenAI Python Library](https://github.com/openai/openai-python)
- [Pricing Calculator](https://openai.com/pricing)

## 🔒 Best Practices

1. **ใช้ Environment Variables** สำหรับ API Key
2. **ตั้งค่า timeout** เพื่อป้องกันการรอนาน
3. **จัดการ errors** อย่างเหมาะสม
4. **ติดตาม usage** เพื่อควบคุมค่าใช้จ่าย
5. **ใช้ caching** สำหรับคำถามที่ซ้ำ

---

💡 **Pro Tip**: ใช้ system prompt ที่ชัดเจนเพื่อควบคุมพฤติกรรมของ AI ให้ตรงกับความต้องการ
