# Tesseract OCR

## 📖 คำอธิบายโปรเจค
โปรเจคนี้ใช้ Tesseract OCR สำหรับการแปลงข้อความในภาพเป็นข้อความดิจิทัล (Optical Character Recognition) รองรับทั้งภาษาไทยและภาษาอังกฤษ เหมาะสำหรับการอ่านข้อความจากเอกสาร ป้าย หรือภาพถ่ายต่างๆ

## ✨ ฟีเจอร์หลัก
- ✅ แปลงข้อความในภาพเป็นข้อความดิจิทัล
- ✅ รองรับภาษาไทยและภาษาอังกฤษพร้อมกัน
- ✅ ใช้งานง่าย เพียงระบุ path ของภาพ
- ✅ ใช้ Tesseract OCR Engine ที่มีความแม่นยำสูง
- ✅ รองรับไฟล์ภาพหลายรูปแบบ

## 🛠️ เทคโนโลยีที่ใช้
- **Tesseract OCR** - Engine สำหรับการแปลงข้อความ
- **pytesseract** - Python Wrapper สำหรับ Tesseract
- **Pillow (PIL)** - สำหรับอ่านและประมวลผลภาพ

## 📦 การติดตั้ง

### 1. ติดตั้ง Tesseract OCR
**Windows:**
- ดาวน์โหลดจาก: https://github.com/UB-Mannheim/tesseract/wiki
- ติดตั้งและจดจำ path ที่ติดตั้ง (เช่น `C:\Program Files\Tesseract-OCR\tesseract.exe`)

**Mac:**
```bash
brew install tesseract
```

**Linux:**
```bash
sudo apt-get install tesseract-ocr
sudo apt-get install tesseract-ocr-tha  # สำหรับภาษาไทย
```

### 2. ติดตั้ง Python Packages
```bash
pip install pytesseract pillow
```

## 🔧 การตั้งค่า
แก้ไข path ของ Tesseract ในโค้ด:
```python
pytesseract.pytesseract.tesseract_cmd = 'C:\\Program Files\\Tesseract-OCR\\tesseract.exe'
```

## 🚀 วิธีการใช้งาน
1. ติดตั้ง Tesseract OCR ตามขั้นตอนด้านบน
2. เตรียมภาพที่มีข้อความภาษาไทยและ/หรือภาษาอังกฤษ
3. แก้ไข path ของภาพในบรรทัด:
```python
image_path = 'path_to_image_th.jpg'
```
4. รันไฟล์ `612.py`
5. โปรแกรมจะแสดงข้อความที่อ่านได้จากภาพ

## 📁 ไฟล์ในโปรเจค
- `612.py` - โค้ดหลักสำหรับ OCR

## 🌐 ภาษาที่รองรับ
โปรเจคนี้ตั้งค่าให้อ่านได้ทั้ง:
- ภาษาไทย (`tha`)
- ภาษาอังกฤษ (`eng`)

สามารถเปลี่ยนหรือเพิ่มภาษาได้ที่:
```python
text = pytesseract.image_to_string(image, lang='tha+eng')
```

## 💡 ตัวอย่างการใช้งาน

### อ่านภาษาไทยอย่างเดียว
```python
text = pytesseract.image_to_string(image, lang='tha')
```

### อ่านภาษาอังกฤษอย่างเดียว
```python
text = pytesseract.image_to_string(image, lang='eng')
```

### อ่านหลายภาษา
```python
text = pytesseract.image_to_string(image, lang='tha+eng+chi_sim')
```

## 📝 หมายเหตุ
- ความแม่นยำขึ้นอยู่กับคุณภาพของภาพ
- ภาพที่มีความคมชัด แสงสว่างเพียงพอ จะได้ผลลัพธ์ที่ดีกว่า
- ควรปรับภาพให้เป็นขาว-ดำ หรือ Grayscale ก่อนใช้งานเพื่อความแม่นยำ
- สำหรับภาษาไทย ควรตรวจสอบว่าติดตั้ง Thai Language Pack แล้ว

## 🎯 เทคนิคการเพิ่มความแม่นยำ
1. **ปรับ Contrast และ Brightness** ของภาพ
2. **ลด Noise** ในภาพด้วย Image Processing
3. **ปรับภาพให้ตรง** (Deskew) ถ้าภาพเอียง
4. **เพิ่มขนาดภาพ** (Upscale) ถ้าภาพเล็กเกินไป
5. **แปลงเป็น Binary Image** (ขาว-ดำเท่านั้น)

## ⚙️ การตั้งค่าเพิ่มเติม
```python
# ระบุ Config สำหรับ Tesseract
custom_config = r'--oem 3 --psm 6'
text = pytesseract.image_to_string(image, lang='tha+eng', config=custom_config)
```

**PSM Modes:**
- `0` = Orientation and script detection only
- `3` = Fully automatic page segmentation (default)
- `6` = Uniform block of text
- `7` = Single text line
- `11` = Sparse text
