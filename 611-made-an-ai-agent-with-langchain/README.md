# AI Agent with LangChain

## 📖 คำอธิบายโปรเจค
โปรเจคนี้รวม 2 ตัวอย่างการใช้ LangChain กับ OpenAI GPT-3.5-turbo:

### 1. TikTok Script Generator (611.py)
สร้างสคริปต์ TikTok อัตโนมัติด้วย Sequential Chain - รับหัวข้อแล้วสร้างคำอธิบายและสคริปต์ตามลำดับ

### 2. AI Agent (611-2.py)
AI Agent ที่ฉลาดสามารถค้นหาข้อมูลจากอินเทอร์เน็ต ดึงข้อมูลจากเว็บไซต์ และสรุปเนื้อหาได้อัตโนมัติ

## ✨ ฟีเจอร์หลัก

### 📹 TikTok Script Generator (611.py)
- ✅ สร้างคำอธิบาย TikTok จากหัวข้อ
- ✅ สร้างสคริปต์ TikTok อัตโนมัติ
- ✅ ใช้ Sequential Chain เชื่อมต่อ 2 ขั้นตอน
- ✅ แสดงขั้นตอนการทำงานแบบ Verbose

### 🤖 AI Agent (611-2.py)
### 🤖 AI Agent (611-2.py)
- ✅ ค้นหาข้อมูลจากอินเทอร์เน็ตด้วย DuckDuckGo
- ✅ ดึงเนื้อหาจากเว็บไซต์ใดก็ได้
- ✅ สรุปเนื้อหาด้วย GPT-3.5-turbo
- ✅ ทำงานอัตโนมัติตามคำสั่งที่ได้รับ
- ✅ รองรับการทำงานหลายขั้นตอน (Multi-step)
- ✅ แสดงขั้นตอนการทำงานแบบ Verbose

## 🛠️ เทคโนโลยีที่ใช้
- **LangChain** - Framework สำหรับสร้าง AI Agent และ Chains
- **OpenAI GPT-3.5-turbo** - โมเดล LLM สำหรับประมวลผลภาษา
- **DuckDuckGo** - สำหรับค้นหาข้อมูลจากอินเทอร์เน็ต (611-2.py)
- **BeautifulSoup** - สำหรับ Parse HTML (611-2.py)
- **Requests** - สำหรับดึงข้อมูลจากเว็บไซต์ (611-2.py)

## 📦 การติดตั้ง

### สำหรับ TikTok Script Generator (611.py)
```bash
pip install langchain langchain-openai python-dotenv
```

### สำหรับ AI Agent (611-2.py)
```bash
pip install langchain langchain-openai langchain-community beautifulsoup4 requests python-dotenv duckduckgo-search
```

## 🔑 การตั้งค่า API Key
1. สร้างไฟล์ `.env` ในโฟลเดอร์เดียวกัน
2. เพิ่ม OpenAI API Key:
```
OPENAI_API_KEY=your_api_key_here
```

## 🚀 วิธีการใช้งาน

### วิธีที่ 1: TikTok Script Generator (611.py)

1. **เตรียมไฟล์ prompts.py** (มีให้แล้วในโฟลเดอร์)
2. **ตั้งค่า API Key** ตามขั้นตอนด้านบน
3. **รันโปรแกรม:**
```bash
python 611.py
```
4. **แก้ไขหัวข้อ** ในโค้ดบรรทัด:
```python
script = tiktok_chain.run("cats are cool")  # เปลี่ยนเป็นหัวข้อที่ต้องการ
```

**ตัวอย่างผลลัพธ์:**
```
> Entering new SimpleSequentialChain chain...
[คำอธิบาย TikTok]
[สคริปต์ TikTok แบบเต็ม]
> Finished chain.
```

### วิธีที่ 2: AI Agent (611-2.py)

1. **ตั้งค่า API Key** ตามขั้นตอนด้านบน
2. **รันโปรแกรม:**
```bash
python 611-2.py
```
3. **แก้ไขคำสั่ง** ในโค้ดบรรทัด:
```python
prompt = "Weather in Bangkok today"  # เปลี่ยนเป็นคำสั่งที่ต้องการ
```

## 📁 ไฟล์ในโปรเจค
- `611.py` - TikTok Script Generator (Sequential Chain)
- `611-2.py` - AI Agent (Tools + Agent)
- `prompts.py` - Prompt Templates สำหรับ 611.py
- `README.md` - เอกสารโปรเจค

## 🔧 รายละเอียดเพิ่มเติม

### TikTok Script Generator (611.py)

**วิธีการทำงาน:**
1. รับหัวข้อจากผู้ใช้
2. Chain แรก: สร้างคำอธิบาย TikTok
3. Chain ที่สอง: สร้างสคริปต์จากคำอธิบาย
4. แสดงผลลัพธ์สคริปต์

**ตัวอย่างการปรับแต่ง Prompts:**
```python
# แก้ไขใน prompts.py
description_prompt = PromptTemplate.from_template(
    "Write me a Thai description for a TikTok about {topic}"
)
```

### AI Agent (611-2.py)

### AI Agent (611-2.py)

**Tools ที่ใช้งาน:**

#### 1. DuckDuckGo Search
- ค้นหาข้อมูลจากอินเทอร์เน็ต
- ส่งคืน URLs และข้อความสั้นๆ

#### 2. WebFetcher
- ดึงเนื้อหาจากเว็บไซต์ตาม URL
- Parse HTML ด้วย BeautifulSoup
- ส่งคืนข้อความเนื้อหาเท่านั้น

#### 3. Summarizer
- สรุปเนื้อหาที่ได้รับด้วย GPT-3.5-turbo
- ใช้ LLMChain พร้อม Prompt Template

**Agent Type:**
ใช้ **OPENAI_FUNCTIONS** Agent Type ซึ่ง:
- รองรับ OpenAI Function Calling
- ตัดสินใจใช้ Tools อัตโนมัติ
- จัดการ Multi-step Tasks ได้ดี
- มีประสิทธิภาพสูง

## 💡 ตัวอย่างการใช้งาน

### TikTok Script Generator (611.py)

```python
# เปลี่ยนหัวข้อได้ตามต้องการ
script = tiktok_chain.run("Python programming tips")
# หรือ
script = tiktok_chain.run("วิธีทำกาแฟสดอร่อย")
```

### AI Agent (611-2.py)

#### ค้นหาสภาพอากาศ
```python
prompt = "Weather in Bangkok today"
agent_executor.invoke({"input": prompt})
```

#### สรุปบทความจากเว็บ
```python
prompt = "Fetch and summarize https://example.com/article"
agent_executor.invoke({"input": prompt})
```

#### ค้นหาและสรุป
```python
prompt = "Search for recent AI news and summarize the top result"
agent_executor.invoke({"input": prompt})
```

## 🆚 เปรียบเทียบ 611.py vs 611-2.py

| คุณสมบัติ | 611.py | 611-2.py |
|-----------|---------|----------|
| **ประเภท** | Sequential Chain | AI Agent |
| **การทำงาน** | ทำงานตามลำดับที่กำหนด | ตัดสินใจเลือก Tools เอง |
| **Tools** | ไม่มี | 3 Tools (Search, Fetch, Summarize) |
| **การใช้งาน** | สร้างเนื้อหา TikTok | ค้นหาและสรุปข้อมูล |
| **ความซับซ้อน** | ต่ำ | ปานกลาง-สูง |
| **เหมาะสำหรับ** | สร้างเนื้อหาตามรูปแบบ | งานที่ต้องการข้อมูลจริง |

## 📝 หมายเหตุ
- ต้องมี Internet Connection ในการทำงาน (611-2.py)
- OpenAI API Key ต้องมี Credits เพียงพอ
- `verbose=True` จะแสดงขั้นตอนการทำงานทั้งสองโปรแกรม
- Agent (611-2.py) จะเลือกใช้ Tools ที่เหมาะสมตามคำสั่งที่ได้รับ
- สามารถเพิ่ม Custom Tools เข้าไปใน `tools` list ได้ (611-2.py)

## ⚠️ ข้อควรระวัง
- อย่าเผยแพร่ API Key
- ระวังค่าใช้จ่ายจาก OpenAI API
- Web Fetcher (611-2.py) อาจไม่ทำงานกับบางเว็บไซต์ที่มี Anti-bot
- การรันบ่อยๆ อาจทำให้เกิน Rate Limit ของ API
