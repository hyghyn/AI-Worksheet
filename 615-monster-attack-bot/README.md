# 615 - Monster Attack Bot

![Python](https://img.shields.io/badge/Python-3.x-blue)
![PyAutoGUI](https://img.shields.io/badge/PyAutoGUI-Automation-green)
![Keyboard](https://img.shields.io/badge/Keyboard-Control-orange)

บอทอัตโนมัติสำหรับการคลิกและโจมตีมอนสเตอร์ในเกม โดยใช้การตรวจจับภาพหน้าจอและคลิกเมาส์อัตโนมัติ

## 📖 คำอธิบาย

โปรเจคนี้เป็นบอทอัตโนมัติที่ใช้ตรวจจับภาพมอนสเตอร์บนหน้าจอและคลิกโจมตีอัตโนมัติ เหมาะสำหรับเกมที่ต้องการคลิกซ้ำๆ เช่น idle games หรือ clicker games

## ✨ ฟีเจอร์

- ✅ ตรวจจับภาพมอนสเตอร์บนหน้าจอแบบเรียลไทม์
- ✅ คลิกเมาส์อัตโนมัติเมื่อพบเป้าหมาย
- ✅ กำหนดพื้นที่การค้นหาได้ (Region of Interest)
- ✅ ปรับระดับความแม่นยำได้ (Confidence Level)
- ✅ หยุดการทำงานได้ด้วยการกดปุ่ม 'Q'
- ✅ แสดงสถานะการค้นหาแบบเรียลไทม์

## 🔧 เทคโนโลยีที่ใช้

- **PyAutoGUI** - ควบคุมเมาส์และตรวจจับภาพหน้าจอ
- **Keyboard** - ตรวจจับการกดปุ่มคีย์บอร์ด
- **OpenCV** - ประมวลผลภาพ (ทางอ้อมผ่าน PyAutoGUI)

## 📦 การติดตั้ง

```bash
pip install pyautogui keyboard pillow
```

## 🚀 การใช้งาน

### 1. เตรียมภาพมอนสเตอร์
- บันทึกภาพหน้าจอของมอนสเตอร์ที่ต้องการโจมตี
- ตั้งชื่อไฟล์เป็น `images.jpg`
- วางไฟล์ไว้ในโฟลเดอร์เดียวกับสคริปต์

### 2. รันโปรแกรม

```python
python 615.py
```

### 3. การใช้งาน
- โปรแกรมจะหน่วงเวลา 2 วินาที เพื่อให้เตรียมตัว
- เปิดหน้าจอเกมที่ต้องการให้บอททำงาน
- บอทจะเริ่มค้นหาและคลิกโจมตีอัตโนมัติ
- กดปุ่ม **'Q'** เพื่อหยุดการทำงาน

## 📝 โค้ดสำคัญ

```python
import time
import keyboard
import pyautogui

time.sleep(2)  # Delay เพื่อเตรียมตัว

while True:
    if keyboard.is_pressed('q'):  # กด Q เพื่อหยุด
        break
    try:
        # ค้นหาภาพบนหน้าจอ
        start = pyautogui.locateCenterOnScreen('images.jpg',
                                               region=(0, 0, 1920, 1080),
                                               grayscale=False,
                                               confidence=0.70)
        if start is not None:
            pyautogui.moveTo(start)  # เลื่อนเมาส์
            pyautogui.click()        # คลิก
            print("Found and clicked!")
    except pyautogui.ImageNotFoundException:
        print("Image not found")
```

## ⚙️ การปรับแต่ง

### Region (พื้นที่การค้นหา)
```python
region=(0, 0, 1920, 1080)  # (x, y, width, height)
```
- ปรับตามความละเอียดหน้าจอของคุณ
- จำกัดพื้นที่ช่วยเพิ่มความเร็วในการค้นหา

### Confidence (ความแม่นยำ)
```python
confidence=0.70  # ค่าระหว่าง 0.0 - 1.0
```
- **0.70** = แนะนำสำหรับการใช้งานทั่วไป
- **0.80-0.90** = สำหรับภาพที่ต้องการความแม่นยำสูง
- **0.60** = สำหรับภาพที่มีการเปลี่ยนแปลงเล็กน้อย

### Grayscale (ขาวดำ)
```python
grayscale=False  # True = เร็วขึ้น แต่แม่นยำน้อยลง
```

## 🎯 Use Cases

1. **Idle/Clicker Games** - คลิกมอนสเตอร์อัตโนมัติ
2. **Farming Games** - เก็บเกี่ยวทรัพยากร
3. **Mobile Game Emulator** - เล่นเกมมือถือบน PC
4. **Grinding/Leveling** - เลเวลขึ้นอัตโนมัติ

## ⚠️ ข้อควรระวัง

1. **ความละเอียดหน้าจอ** - ต้องตรงกับ region ที่กำหนด
2. **คุณภาพภาพ** - ภาพที่ใช้ต้องชัดเจนและไม่เบลอ
3. **Terms of Service** - ตรวจสอบเงื่อนไขของเกมก่อนใช้งาน
4. **CPU Usage** - การค้นหาภาพใช้ CPU สูง อาจทำให้เครื่องร้อน

## 🐛 การแก้ปัญหา

### ไม่พบภาพ (Image not found)
- ✅ ตรวจสอบว่าไฟล์ `images.jpg` อยู่ในโฟลเดอร์เดียวกัน
- ✅ ลด confidence เป็น 0.60-0.65
- ✅ ถ่ายภาพใหม่ให้ชัดเจนขึ้น
- ✅ ตรวจสอบ region ว่าครอบคลุมพื้นที่ที่ถูกต้อง

### คลิกผิดตำแหน่ง
- ✅ เพิ่ม confidence เป็น 0.80-0.85
- ✅ ถ่ายภาพที่มีขนาดเล็กลงและเฉพาะเจาะจง
- ✅ ตรวจสอบว่าภาพไม่มีสิ่งรบกวน

### โปรแกรมทำงานช้า
- ✅ ลด region ให้เล็กลง
- ✅ เปิด grayscale=True
- ✅ เพิ่ม time.sleep() เล็กน้อยในลูป

## 📚 เอกสารเพิ่มเติม

- [PyAutoGUI Documentation](https://pyautogui.readthedocs.io/)
- [Keyboard Documentation](https://github.com/boppreh/keyboard)

## 📄 License

โปรเจคนี้ใช้สำหรับการศึกษาเท่านั้น กรุณาใช้อย่างรับผิดชอบและเคารพกฎของเกม

---

💡 **Pro Tip**: ใช้ `pyautogui.screenshot()` เพื่อถ่ายภาพหน้าจอและหาตำแหน่งที่แม่นยำก่อนใช้งานจริง
