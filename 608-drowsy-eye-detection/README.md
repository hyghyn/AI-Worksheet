# Drowsy Eye Detection

## 📖 คำอธิบายโปรเจค
โปรเจคนี้ใช้ MediaPipe Face Landmarker สำหรับตรวจจับการหลับตาและเตือนภาวะง่วงนอน ด้วยการวิเคราะห์จุดสำคัญรอบดวงตาและคำนวณระดับการเปิด-ปิดของเปลือกตา เหมาะสำหรับระบบเตือนผู้ขับขี่ที่ง่วงนอน

## ✨ ฟีเจอร์หลัก
- ✅ ตรวจจับจุดสำคัญบนใบหน้าทั้งหมด (478 จุด)
- ✅ วิเคราะห์ดวงตาทั้งสองข้างแบบเรียลไทม์
- ✅ คำนวณระดับการเปิดของเปลือกตา
- ✅ ตรวจจับภาวะง่วงนอนเมื่อตาหลับนานเกิน 20 เฟรม
- ✅ แสดงค่าความสูงของดวงตาแบบเรียลไทม์
- ✅ แสดงการเตือน ALERT เมื่อตรวจพบภาวะง่วง

## 🛠️ เทคโนโลยีที่ใช้
- **OpenCV** - สำหรับการประมวลผลภาพและแสดงผลจากกล้อง
- **MediaPipe** - สำหรับการตรวจจับจุดสำคัญบนใบหน้า
- **NumPy** - สำหรับการคำนวณทางคณิตศาสตร์

## 📦 การติดตั้ง
```bash
pip install opencv-python mediapipe numpy
```

## 🚀 วิธีการใช้งาน
1. ตรวจสอบให้แน่ใจว่ามีไฟล์ `face_landmarker.task` อยู่ในโฟลเดอร์เดียวกัน
2. รันไฟล์ `608.py`
3. กล้องเว็บแคมจะเปิดขึ้น
4. โปรแกรมจะเริ่มตรวจจับใบหน้าและดวงตา
5. ลองหลับตาค้างไว้เพื่อทดสอบระบบเตือน
6. กด **ESC** เพื่อออกจากโปรแกรม

## 📁 ไฟล์ในโปรเจค
- `608.py` - โค้ดหลักสำหรับตรวจจับภาวะง่วงนอน
- `face_landmarker.task` - โมเดล Face Landmarker

## 🔬 วิธีการทำงาน
1. **ตรวจจับใบหน้า**: ใช้ Face Landmarker หาจุดสำคัญบนใบหน้า 478 จุด
2. **แยกดวงตา**: ดึงจุดสำคัญของดวงตาซ้าย (16 จุด) และขวา (16 จุด)
3. **คำนวณความสูง**: วัดระยะแนวตั้งระหว่างเปลือกตาบนและล่าง
4. **เปรียบเทียบ**: เทียบความสูงปัจจุบันกับค่าสูงสุดที่บันทึกไว้
5. **ตรวจจับ**: ถ้าความสูงน้อยกว่า 50% ของค่าสูงสุด = ตาหลับ
6. **เตือน**: หากตาหลับนานกว่า 20 เฟรมติดต่อกัน แสดงข้อความ "ALERT"

## ⚙️ การปรับแต่ง
สามารถปรับค่าต่างๆ ได้:
- `drowsy_frames > 20` - จำนวนเฟรมก่อนเตือน (ปรับตามความเหมาะสม)
- `int(max_left/2)+1` - เกณฑ์การปิดตา (ปัจจุบันตั้งที่ 50%)
- `min_face_detection_confidence=0.5` - ความมั่นใจในการตรวจจับใบหน้า

## 📝 หมายเหตุ
- โปรแกรมจะเรียนรู้ค่าความสูงสูงสุดของดวงตาแต่ละข้าง (max_left, max_right)
- วงกลมสีเหลือง = จุดสำคัญทั้งหมดบนใบหน้า (478 จุด)
- เส้นสีเขียว = กรอบรอบดวงตา
- จุดสีแดง = จุดสำคัญรอบดวงตา
- ข้อความสีเขียว = แสดงค่าความสูงของดวงตาแบบเรียลไทม์
